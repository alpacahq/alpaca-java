# Pinned OpenAPI documents include upstream example payloads (e.g. expired
# pre-signed S3 URLs) that trip secret detectors. Real secrets do not belong in
# these files; ignore them so adopt/regen commits are not blocked.
paths-ignore:
  - "specs/**/openapi.yaml"
